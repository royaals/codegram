import { parseQuery, stringifyQuery } from "ufo";
export const parse = function(obj, _sep, _eq, _options) {
  return parseQuery(obj);
};
export const decode = parse;
export const stringify = function(obj, _sep, _eq, _options) {
  return stringifyQuery(obj);
};
export const encode = stringify;
export let escape = encodeURIComponent;
export let unescape = decodeURIComponent;
export const unescapeBuffer = function(str, _decodeSpaces) {
  return Buffer.from(unescape(str));
};
export default {
  decode,
  encode,
  escape,
  parse,
  stringify,
  unescape,
  unescapeBuffer
};
