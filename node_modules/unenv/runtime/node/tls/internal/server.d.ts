/// <reference types="node" />
/// <reference types="node" />
/// <reference types="node" />
import type tls from "node:tls";
import { Server as _Server } from "node:net";
export declare class Server extends _Server implements tls.Server {
    addContext(hostname: string, context: tls.SecureContextOptions): void;
    setSecureContext(options: tls.SecureContextOptions): void;
    setTicketKeys(_keys: Buffer): void;
    getTicketKeys(): Buffer;
}
