/// <reference types="node" />
/// <reference types="node" />
import { EventEmitter } from "node:events";
import type worker_threads from "node:worker_threads";
export declare class MessagePort extends EventEmitter// eslint-disable-line unicorn/prefer-event-target
 implements worker_threads.MessagePort {
    close(): void;
    postMessage(value: any, transferList?: readonly worker_threads.TransferListItem[] | undefined): void;
    ref(): void;
    unref(): void;
    start(): void;
}
