/// <reference types="node" />
import { MessagePort } from "./message-port";
import type worker_threads from "node:worker_threads";
export declare class MessageChannel implements worker_threads.MessageChannel {
    port1: MessagePort;
    port2: MessagePort;
}
